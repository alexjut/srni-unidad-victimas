# ============================================================
# SRNI Backend — Requirements
# Python 3.14 compatible
# ============================================================

# --- Core ---
Django==5.2.15                         # LTS — soporta Python 3.13/3.14
djangorestframework==3.15.2
djangorestframework-simplejwt==5.4.0
drf-spectacular==0.28.0             # Esquema OpenAPI (referenciado en INSTALLED_APPS)
django-cors-headers==4.4.0
django-filter==24.3
django-unfold==0.98.0               # Tema moderno del panel de administración

# --- Base de datos ---
# NOTA: psycopg2-binary no tiene wheels para Python 3.14.
# Se usa psycopg (v3) con driver binario — API compatible con adaptaciones mínimas.
psycopg[binary]==3.2.3

# --- Cifrado campos PII ---
# django-encrypted-model-fields 0.6.5 no tiene soporte declarado para Python 3.14.
# Se usa cryptography directamente; campo personalizado en apps/victimas/fields.py
cryptography==44.0.2
# django-encrypted-model-fields==0.6.5  # OMITIDO — incompatible Python 3.14

# --- Hash de contraseñas (Argon2) ---
argon2-cffi==23.1.0                 # Requerido por Django para Argon2PasswordHasher

# --- Seguridad ---
django-ratelimit==4.1.0

# --- Caché y tareas asíncronas ---
redis==5.2.1
celery==5.4.0
django-redis==5.4.0

# --- Almacenamiento de documentos (MinIO/S3) ---
boto3==1.35.86
django-storages==1.14.4

# --- Utilidades ---
python-decouple==3.8
Pillow==11.1.0
django-auditlog==3.0.0

# --- Desarrollo y testing ---
pytest==8.3.4
pytest-django==4.9.0
factory-boy==3.3.1
coverage==7.6.10

# --- IA — Gemini ---
google-generativeai==0.8.3          # Proxy backend → Gemini API (clave NUNCA en móvil)

# --- Oracle legacy (RNIENTREVISTA) ---
# Cliente "thin": NO requiere Oracle Instant Client ni librerías del sistema, así que
# la imagen no cambia de base. Lo usan la carga del padrón (victimas) y la escritura
# de caracterizaciones vía procedures GIC_* (sincronizacion).
oracledb==4.0.1

# --- Producción ---
gunicorn==23.0.0
whitenoise==6.8.2
