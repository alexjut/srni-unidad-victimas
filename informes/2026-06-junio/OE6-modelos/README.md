# OE6 — Modelos de datos documentados

> **Obligación contractual:** *Crear y documentar modelos de datos que reflejen con precisión la información que se desea analizar, considerando las relaciones entre los diferentes conjuntos de datos en las soluciones tecnológicas y aplicativos móviles.*

## Actividad desarrollada en este periodo

*(Pendiente — se completa al cierre del mes.)*

**Frente que se ejecuta este mes:**

- Creación de la carpeta `docs/adr/` con los **Architecture Decision Records** de las decisiones técnicas más relevantes que aún no estaban formalizadas:
  - ADR — PostgreSQL local en lugar de Oracle directo (estrategia "tablas nuevas").
  - ADR — Idempotencia 1 víctima → 1 hogar (excepción documentada para víctima de pruebas).
  - ADR — Fix Hermes Math.random fallback para UUID en la app móvil.
  - ADR — Despliegue con Docker Compose en el servidor UARIV.
  - ADR — Auditoría inmutable a nivel de motor (revoke UPDATE/DELETE).
- Actualización de `docs/estado-actual.md` con corte al cierre de junio.
- Refresco de `docs/arquitectura/ARQUITECTURA.md` con la versión vigente del stack.

## Evidencia que soporta esta actividad

*(Pendiente — archivos ADR en `docs/adr/`, snapshot final de `docs/estado-actual.md`.)*
